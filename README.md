# Guess the Weather

The app sends a request to access the API Open-Meteo, which turns a city name into coordinates, then gets the city's current weather as text data. No API keys were needed, as it is free and available for everyone. To run it, a user would open index.html in the browser.
